package interfaces;
/*
Uma interface define uma série de métodos que devem ser criados
nas classes que a implementam como um contrato
*/
public interface Autenticavel {
    boolean autentica(int senha);
}
